server:
  port: 8080

app:
  name: "Task Manager Application"
  version: "1.0.0"
  description: "Homework"
  created-by: "Tim"
  upload:
    dir: uploads

api:
  version: "2.0.0"

logging:
  level:
    com.mipt.hw: DEBUG

spring:
  application:
    name: HW2ndSem
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

  profiles:
    active: dev